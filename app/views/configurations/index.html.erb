<p id="notice"><%= notice %></p>

<h1>Configurations</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Username</th>
      <th>Website</th>
      <th>Biography</th>
      <th>Phone</th>
      <th>Privateaccount</th>
      <th>User</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @configurations.each do |configuration| %>
      <tr>
        <td><%= configuration.name %></td>
        <td><%= configuration.username %></td>
        <td><%= configuration.website %></td>
        <td><%= configuration.biography %></td>
        <td><%= configuration.phone %></td>
        <td><%= configuration.privateAccount %></td>
        <td><%= configuration.user %></td>
        <td><%= link_to 'Show', configuration %></td>
        <td><%= link_to 'Edit', edit_configuration_path(configuration) %></td>
        <td><%= link_to 'Destroy', configuration, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Configuration', new_configuration_path %>
